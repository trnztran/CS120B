void Detect_Max_Amp()
{
    switch(detect_max_amp_state)
    {
        case DMAInit:
            break;
        
        default:
            break;
    }
    switch(detect_max_amp_state)
    {
        case DMAInit:
            break;
        default:
            break;
    }
}
